
Add epoch_starts to all svgd paths
Add ability to tie parameter estimation across epochs, so only a single value is estimated. Useful when epochs only serve to condition on known fixed population sizes.