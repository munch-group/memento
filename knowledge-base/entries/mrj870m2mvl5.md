Can phasic express an end-point conditioned Markov chain to produce a K  x K matrix where cell i,j is the reward earned starting in i and ending in j after time t? For each i, j pair, could I just use an IPV starting only at i, only allow state j to go to absorbing , and then reward the sojourn_times after time t? If that works, I could have a graph for each i, j combination and then reuse that for different t.
